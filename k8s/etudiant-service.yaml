apiVersion: v1
kind: Service
metadata:
  name: etudiant-service
spec:
  type: NodePort
  selector:
    app: etudiant-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080